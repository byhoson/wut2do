<header>
        <a href="/"><img src="/img/logo.png" alt="logo"></a>
        <% if(signed_in) { %>
                <a href="/auth/signout">Sign Out</a>
        <% } else { %>
                <a href="/auth">Sign In</a>
        <% } %>
</header>