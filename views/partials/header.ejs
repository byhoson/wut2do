<header>
        <a href="/"><img src="/img/logo.png" alt="logo"></a>
        <% if(username) { %>
                <span><%= username %></span>
                <a href="/auth/signout">Sign Out</a>
        <% } else { %>
                <a href="/auth">Sign In / Sign Up</a>
        <% } %>
</header>